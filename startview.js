//login함수
function login(str){
  location.replace(str);
}
//회원가입 함수
function signUp(str) {
  location.replace(str);
}
//회원가입 완료 함수
function enterSignUp(str) {
  location.replace(str);
}
//login완료 함수
function enterLogin(str){
  location.replace(str);
}